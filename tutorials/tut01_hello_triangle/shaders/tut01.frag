#version 330

out vec4 outColor;

void main() {
	// Edit this line while the tutorial is running -- once shader hot-reload is
	// wired up the triangle changes color the moment you save.
	outColor = vec4(1.0, 0.45, 0.2, 1.0);
}
